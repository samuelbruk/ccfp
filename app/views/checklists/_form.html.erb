<%= form_for([@card, @checklist], remote: true) do |f| %>
  <%= f.text_field(:content, placeholder: 'Card Name', class: 'form-control') %>
  <%= f.submit('Submit', class: 'btn btn-default') %>
<% end %>
