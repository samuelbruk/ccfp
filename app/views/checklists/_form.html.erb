<%= form_for([@card, @checklist], remote: true) do |f| %>
  <%= f.text_field(:content, placeholder: 'Add checklist here...', class: 'form-control') %>
  <%= f.submit('Add', class: 'btn btn-success') %>
<% end %>
